//condição ? true : false;
var x = 1;
var y = 2;

x === y ? 'igual' : 'diferente';

if (x === y){
    return 'igual';
} else {
    return 'diferente';
}

var sexo = 'M';     //pessoa.sexo da aula de objetos

var artigo = (sexo === 'M') ? 'o' : 'a';

